export default function TotpCodeInput({
  label,
  placeholder,
  showError,
  errorMessage,
}: {
  label?: string;
  placeholder: any;
  showError: any;
  errorMessage?: string;
}): import("react").JSX.Element;
